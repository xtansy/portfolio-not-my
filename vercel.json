{
    "builds": [
        {
            "src": "gulpfile.js",
            "use": "@vercel/static-build",
            "config": {
                "buildCommand": "gulp --prod",
                "outputDirectory": "public"
            }
        }
    ]
}